<%@ page import="common.vo.SessionInfo" %>
<%
/*
    SessionInfo sessionInfo = (SessionInfo)(session.getAttribute("sessionInfo"));    
    // 사용자 ID

    sessionInfo.setUsr_Cd(request.getParameter("usr_cd")); 
    //  ( P:???, G:??)
    sessionInfo.setSub_System(request.getParameter("sub_system")); 
    //? ??
    sessionInfo.setStr_Cd(request.getParameter("str_cd")); 
    //????
    sessionInfo.setJojik_Gb(request.getParameter("jojik_gb")); 
    
    //?
    if (request.getParameter("bu") != null) { 
        sessionInfo.setBu(request.getParameter("bu")); 
    }

    //?
    if (request.getParameter("gwa") != null) { 
        sessionInfo.setGwa(request.getParameter("gwa")); 
    }

    //?
    if (request.getParameter("kye") != null) {
        sessionInfo.setKye(request.getParameter("kye")); 
    }

    
    session.setAttribute("sessionInfo", sessionInfo);
    
    SessionInfo sessionInfo2 = (SessionInfo)(session.getAttribute("sessionInfo"));    
    System.out.println("temp : " + sessionInfo2.getStr_Cd());
    
*/
%>

